// Auto-expire high-risk entitlements after 90 days
if (entitlement.getAttribute("riskLevel") != null && 
    "High".equals(entitlement.getAttribute("riskLevel"))) {
    Calendar cal = Calendar.getInstance();
    cal.add(Calendar.DATE, 90);
    item.setExpiration(cal.getTime());
}
